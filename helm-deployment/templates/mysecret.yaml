apiVersion: v1
kind: Secret
metadata:
  name: registrypullsecret
  namespace: {{ .Values.namespace.name }}
data:
  .dockerconfigjson: {{ .Values.secret.securestring }}
type: kubernetes.io/dockerconfigjson